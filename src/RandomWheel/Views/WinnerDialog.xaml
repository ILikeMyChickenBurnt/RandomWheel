<Window x:Class="RandomWheel.Views.WinnerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Winner!" 
        Width="500" Height="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    
    <!-- Dialog Content -->
    <Border Background="{StaticResource SurfaceBrush}" 
            CornerRadius="16" 
            BorderBrush="{StaticResource PrimaryBrush}" 
            BorderThickness="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Width="450">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.4" Color="#000000"/>
        </Border.Effect>
        
        <Grid Margin="32,32,32,28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Trophy Icon -->
            <Border Grid.Row="0" 
                    Width="72" Height="72" 
                    CornerRadius="36" 
                    Background="{StaticResource PrimaryBrush}"
                    HorizontalAlignment="Center" 
                    Margin="0,0,0,12">
                <TextBlock Text="&#x2605;" 
                           FontSize="36" 
                           Foreground="White"
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"/>
            </Border>
            
            <!-- Title -->
            <TextBlock Grid.Row="1" 
                       Text="We Have a Winner!" 
                       FontFamily="{StaticResource DefaultFontFamily}"
                       FontSize="20" 
                       FontWeight="Medium"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16"/>
            
            <!-- Winner Name - Large and prominent, white background -->
            <TextBlock Grid.Row="2"
                       x:Name="WinnerNameText"
                       Text="Winner Name"
                       FontFamily="{StaticResource DefaultFontFamily}"
                       FontSize="42"
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Padding="8,4"
                       Margin="0,0,0,16"/>
            
            <!-- Question and Buttons -->
            <StackPanel Grid.Row="3">
                <TextBlock Text="Mark as selected and hide from future spins?"
                           FontFamily="{StaticResource DefaultFontFamily}"
                           FontSize="14"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="YesButton" 
                            Content="Yes, Mark Selected" 
                            Style="{StaticResource ModernButtonStyle}"
                            FontSize="14"
                            Padding="24,12"
                            Click="YesButton_Click"
                            IsDefault="True"/>
                    
                    <Button x:Name="NoButton" 
                            Content="No, Keep Active" 
                            Padding="24,12"
                            FontSize="14"
                            Margin="16,0,0,0"
                            Click="NoButton_Click"
                            IsCancel="True">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
                                <Setter Property="Background" Value="#E53935"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#C62828"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
